---
interface Props { offset?: string; id?: string; }
const { offset = '100px', id } = Astro.props;
---
<section class="s-target" style={`scroll-margin-top: ${offset}`} id={id}>
  <slot />
</section>
<style>
  .s-target { position: relative; }
</style>
